# Direct dependencies only, with lower bounds — pip resolves the rest.
# (The previous file was `pip freeze` output from a dev box whose index had
#  post-dated builds that don't exist on public PyPI.)

Django>=5.1,<5.3
djangorestframework>=3.15
djangorestframework-simplejwt>=5.3
django-filter>=24.3
django-cors-headers>=4.4
drf-spectacular>=0.27
psycopg[binary]>=3.2
gunicorn>=22.0
whitenoise>=6.6
python-dotenv>=1.0
